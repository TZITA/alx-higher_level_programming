#!/usr/bin/python3
def uppercase(str):
    l_len = len(str)
    while i < l_len):
        if 97 <= ord(str[i]) <= 122:
            m = chr(ord(str[i]) + 32)
            print("{}".format(m), end='')
        else:
            continue
        i += 1
    print("{}".format('\n'))
